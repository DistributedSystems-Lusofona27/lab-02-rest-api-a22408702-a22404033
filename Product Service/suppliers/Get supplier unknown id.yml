info:
  name: Get supplier unknown id
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/api/v1/suppliers/ffffffff-ffff-ffff-ffff-ffffffffffff"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
