info:
  name: Create validação
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/api/v1/products"
  body:
    type: json
    data: '{"name":"","sku":"kb 87","price":-5,"stock":-1}'
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
