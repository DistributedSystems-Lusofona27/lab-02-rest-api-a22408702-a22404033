info:
  name: Get id desconhecido
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/api/v1/products/ffffffff-ffff-ffff-ffff-ffffffffffff"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
